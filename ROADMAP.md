# ROADMAP — 분석 엄밀성 + UI 개선

각 단계는 독립적으로 끝낼 수 있게 쪼개져 있다. 단계마다 **완료 조건**이 있고,
그걸 통과 못 하면 다음 단계로 넘어가지 않는다.

---

## A. 표본 크기 보정 (shrinkage)

### 문제
현재 600분 뛴 선수와 2900분 뛴 선수의 per-90 값을 같은 무게로 취급한다.
600분은 대략 7경기 분량이고, 그 구간의 npxG/90은 절반이 운이다.
실제로 25/26 결정력 순위 3위가 정확히 600분만 뛴 선수였다.

### 방법 — 경험적 베이즈 축소
```
조정값 = (실제값 × 분 + 사전평균 × K) / (분 + K)
```
- `사전평균` = 같은 포지션 그룹(FW/MF/DF)의 출전시간 가중 평균
- `K` = 실제값과 사전평균을 50:50으로 섞는 분 수. 지표가 안정될수록 작다.

### K 기본값 (출발점 — B단계에서 튜닝한다)
| 지표 | K | 근거 |
|---|---|---|
| `sh90` | 400 | 슈팅 빈도는 빨리 안정된다 |
| `kp90` | 600 | |
| `xGChain90` | 700 | |
| `xGBuildup90` | 700 | |
| `npxG90` | 900 | |
| `xA90` | 1400 | 도움 계열은 가장 느리게 안정된다 |

**K를 직접 추정하는 쪽이 더 좋다.** 연속 두 시즌 모두 600분 이상 뛴 선수들을 모아
시즌1 값과 시즌2 값의 상관계수 `r`을 지표마다 구한 뒤 `K ≈ 평균출전분 × (1−r)/r`.
데이터가 3~4시즌 있으니 가능하다. 추정한 값과 위 기본값이 크게 다르면 추정값을 쓴다.

### 구현
- `build.py`에서 per-90 계산 직후, **백분위를 매기기 전에** 축소를 적용한다.
- 원값도 함께 내보낸다: `npxG90`(원값, 표에 표시) / `npxG90_adj`(축소값, 백분위·역할점수 계산용).
- 사이트에서 축소값 기반임을 한 줄로 밝힌다. 숨기지 않는다.

### 완료 조건
- 600분 선수의 역할 점수가 눈에 띄게 내려간다 (말런이 3위에서 밀려나야 정상)
- 2900분 선수의 값은 거의 안 변한다 (분 ≫ K 이므로)
- 모든 `_adj` 값이 원값과 사전평균 사이에 있다

---

## B. 백테스트 — 이 모델이 실제로 예측력이 있나

### 목적
"24/25 역할 점수가 높았던 선수가 25/26에 실제로 잘했나"를 수치로 보인다.
이게 있어야 모델이 주장이 아니라 근거가 된다.

### 방법
1. 24/25와 25/26 **둘 다** 600분 이상 뛴 선수를 뽑는다 (양 리그 합쳐 200~300명 예상)
2. x축 = 24/25 역할 점수, y축 = 25/26 실제 (npxG+xA)/90
3. 피어슨 `r`과 스피어만 `ρ`를 구한다
4. **비교군을 반드시 같이 계산한다** — 이게 핵심이다:
   - 축소 적용 전 역할 점수 vs 적용 후 → 축소가 예측력을 올렸는지
   - 단순 "지난 시즌 골 수"만 썼을 때의 `r` → 우리 모델이 그보다 나은지
   - 나은 게 없으면 그 사실을 그대로 쓴다. 좋게 포장하지 않는다.
5. 3시즌 있으면 23/24→24/25도 같이 돌려서 한 번의 우연이 아닌지 본다

### 산출물
- `scripts/backtest.py` — 결과를 `docs/backtest.json`으로 저장
- 사이트에 "6. 모델 검증" 섹션 추가: 산점도 + `r` 값 + 비교군 표
- 산점도는 점이 300개쯤 되니 반투명 + 추세선, 상위 5명만 이름 라벨

### 완료 조건
- 축소 적용 후 `r`이 적용 전보다 높다 (아니면 A단계 K를 다시 잡는다)
- 비교군 `r`이 모두 표에 있다

---

## C. 리그 강도 보정

### 문제
라리가 0.50 npxG/90과 PL 0.50을 지금은 그냥 같다고 친다. 두 리그 난이도가 다르면 틀린다.

### 방법 — 이적 선수 전후 비교
1. N시즌 리그 A에서 600분+, N+1시즌 리그 B에서 600분+ 뛴 선수를 찾는다
2. 지표별로 `이후/이전` 비율을 구한다
3. 중앙값을 보정 계수로 쓰고, **부트스트랩으로 95% 신뢰구간**을 낸다
4. **신뢰구간이 1.0을 포함하면 보정하지 않는다 (계수 = 1.0).**
   PL↔라리가 이적자는 3시즌 합쳐도 20~40명 수준이라 표본이 작다.
   작은 표본으로 억지 보정하는 것보다 "차이를 확인 못 했다"가 정직하다.
5. 나이 효과가 섞인다는 걸 안다 (이적은 보통 커리어 특정 시점에 일어난다).
   한계로 명시한다.

### 완료 조건
- 표본 크기와 신뢰구간이 `docs/data.json` meta에 들어간다
- 사이트 방법론 섹션에 계수와 표본 수가 표시된다
- 표본이 15명 미만이면 보정을 끄고 그 사실을 표시한다

---

## D. 유사 선수 검색

### 방법
- 축소된 6개 지표를 각각 z-표준화 → 6차원 벡터
- 코사인 유사도, 같은 포지션 그룹 안에서만
- 상위 8명, 본인과 같은 팀은 제외

### UI
선수 행을 펼쳤을 때 "비슷한 선수" 줄을 추가. 이름을 누르면 그 선수로 이동.

---

## E. 스타일 적합도

### 팀 스타일 분류
`ppda`, `ppda_allowed`, `deep90`, `deep_allowed90`, `npxG90` 을 z-표준화해서
k-means(k=4). 클러스터에 사람이 읽을 이름을 붙인다 (예: 고압박·점유형 / 저블록·역습형 ...).
이름은 클러스터 중심값을 보고 정한다. 미리 정해놓고 끼워맞추지 않는다.

### 선수 적합도 — 약한 지표임을 인정할 것
Understat 지표로는 선수 개인의 전술 성향을 알 수 없다.
그래서 **"현재 소속팀 스타일이 목표팀과 얼마나 비슷한가"**를 적응 리스크의 대리 지표로 쓴다.
이건 선수 성향 측정이 아니라 근사치다. UI에 그렇게 쓴다.
"적합도 92점" 같은 단정적 점수 대신 "적응 리스크: 낮음/보통/높음" 3단계로 표시한다.

---

## F. 대체 가치

목표팀에서 그 역할의 현재 주전(해당 포지션 그룹 최다 출전자)을 찾아,
후보와의 `(npxG90_adj + xA90_adj)` 차이 × 2500분(한 시즌 주전 기준)을 계산한다.

> "라피냐 영입 시 시즌 기대 생산 +6.2 (현 주전 대비)"

캐비엇을 반드시 붙인다: 팀이 바뀌면 생산량도 바뀐다. 이건 상한 추정치다.

---

## G. URL 상태 공유

`?league=EPL&team=Arsenal&role=finisher&mins=900&pos=FW`
- 컨트롤이 바뀌면 `history.replaceState`로 URL 갱신
- 로드 시 URL 파라미터를 읽어 초기 상태 설정
- 파라미터가 없거나 이상하면 조용히 기본값

---

## H. 디자인 개선

**D~G가 끝난 뒤에 한다.** UI 요소가 다 나온 다음에 한 번에 정리해야 두 번 일 안 한다.

### 지금 뭐가 약한가
1. **전부 카드다.** 모든 블록이 같은 테두리·같은 radius·같은 18px 패딩이라
   시각적 위계가 없다. 중요한 것과 부수적인 것이 똑같아 보인다.
2. **결론에 무게가 없다.** 이 사이트의 결론은 "이 팀엔 이 역할이 필요하고 이 선수가 후보다"인데,
   그게 나머지 표와 같은 크기로 놓여 있다.
3. **백분위 막대가 제일 좋은 요소인데 눈에 안 띈다.** 이게 이 사이트의 시각적 서명이 돼야 한다.
4. 헤더가 평범하고, 넣어둔 세로줄 장식은 보이지도 않는다. 빼거나 제대로 쓴다.
5. 표에 리듬이 없다. 행 높이·정렬·강조가 균일해서 스캔이 안 된다.

### 방향
- **카드를 줄이고 여백과 괘선으로 묶는다.** 테두리는 정말 분리돼야 하는 것에만.
- **위계를 3단으로**: 결론(크게) → 근거 지표(중간) → 부수 정보(작게).
- 백분위 막대를 이 사이트의 언어로 확립한다 — 굵기·눈금·라벨 규칙을 하나로 통일하고
  팀 진단·선수 프로필·검증 섹션에 같은 규칙으로 쓴다.
- 숫자에는 `tabular-nums` 유지. 표는 행 높이를 줄이고 구분선을 얇게.
- 폰트는 지금 조합(Archivo + IBM Plex)이 무난하지만 개성이 없다. 바꿀 거면
  본문은 가독성 좋은 것으로 두고 표제만 성격 있는 것으로 — 단, 한글이 같이 나오므로
  한글 폴백이 어색하지 않은지 반드시 확인한다.

### 절대 깨뜨리면 안 되는 것
- **데이터 로딩 경로 2개** (인라인 payload / fetch) 둘 다 살아 있어야 한다
- **다크모드 토큰 구조** — 색은 전부 `:root` 변수로 정의하고, 미디어쿼리와
  `[data-theme]` 양쪽에 재정의. 미디어쿼리 안에서만 정의된 색이 있으면 한쪽 테마가 깨진다
- `body`에 명시적 배경색 (투명하면 호스트 배경이 비친다)
- 820px 이하 반응형. 표는 자기 컨테이너 안에서 가로 스크롤되고 페이지 본문은 안 밀려야 한다
- 키보드 포커스 표시, `prefers-reduced-motion`

### 완료 조건
라이트/다크 × 데스크톱/모바일 4가지 조합을 실제로 열어서 확인한다.
캡처 없이 "됐을 것"으로 넘어가지 않는다.
