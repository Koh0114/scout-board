# Claude Code 프롬프트 모음

순서대로 하나씩. 각 프롬프트는 새 세션에서 시작해도 되고, 이어서 해도 된다.
전부 `ROADMAP.md`를 참조하므로 프롬프트 자체는 짧다.

---

## 1 — 표본 크기 보정 (ROADMAP A)

```
ROADMAP.md의 A단계를 구현해줘.

먼저 K를 추정하는 쪽으로 가자. 연속 두 시즌 모두 600분 이상 뛴 선수들로
지표별 시즌간 상관계수를 구하고, 거기서 K를 계산해줘. 표본이 50명 미만인
지표는 ROADMAP의 기본값을 쓰고 그 사실을 출력해줘.

build.py에서 per-90 계산 직후·백분위 계산 전에 축소를 적용하고,
원값(표시용)과 축소값(백분위·역할점수용)을 둘 다 data.json에 넣어줘.

끝나면 A단계 완료 조건 3개를 실제로 확인해서 숫자로 보여줘.
특히 Donyell Malen(600분)의 결정력 순위가 축소 전후로 어떻게 바뀌는지.
```

## 2 — 백테스트 (ROADMAP B)

```
ROADMAP.md의 B단계를 구현해줘. scripts/backtest.py 새로 만들고
결과는 docs/backtest.json으로.

중요한 건 비교군이야. 축소 전/후, 그리고 "지난 시즌 골 수만 쓴 경우"까지
셋 다 r을 계산해서 나란히 보여줘. 우리 모델이 단순 골 수보다 못하면
그대로 보고해줘 — 그러면 A단계 K를 다시 잡아야 하는 신호니까.

23/24→24/25와 24/25→25/26 둘 다 돌려서 한 번의 우연이 아닌지 확인.

결과를 사이트에 "6. 모델 검증" 섹션으로 붙이되, 지금은 기능만 동작하면 돼.
디자인은 나중에 H단계에서 전체적으로 손볼 거야.
```

## 3 — 리그 강도 보정 (ROADMAP C)

```
ROADMAP.md의 C단계를 구현해줘.

PL↔라리가를 오간 선수 표본이 작을 거야. 부트스트랩 신뢰구간을 꼭 내고,
1.0을 포함하면 보정하지 말고 계수를 1.0으로 둬. 표본 15명 미만이면 아예 끄고.

표본 크기·계수·신뢰구간을 data.json meta에 넣고 사이트 방법론 섹션에 표시해줘.
나이 효과가 섞여 있다는 한계도 같이 써줘.

실제로 몇 명이 잡혔고 계수가 얼마 나왔는지 알려줘.
```

## 4 — 유사 선수 + 스타일 적합도 (ROADMAP D, E)

```
ROADMAP.md의 D단계와 E단계를 구현해줘.

D는 선수 행을 펼쳤을 때 "비슷한 선수" 8명이 나오고, 이름을 누르면 그 선수로 이동.

E는 조심해서. Understat으로는 선수 개인의 전술 성향을 알 수 없으니까
"적합도 92점" 같은 단정적 점수 만들지 말고, ROADMAP에 쓴 대로
"적응 리스크: 낮음/보통/높음" 3단계 + 이게 대리 지표라는 설명으로 가줘.

k-means 클러스터 이름은 실제 중심값을 출력해서 보여주고, 그거 보고 같이 정하자.
미리 이름 정해놓고 끼워맞추지 말고.
```

## 5 — 대체 가치 + URL 공유 (ROADMAP F, G)

```
ROADMAP.md의 F단계와 G단계를 구현해줘.

F는 캐비엇이 중요해. "팀이 바뀌면 생산량도 바뀌므로 상한 추정치"라는 걸
숫자 바로 옆에 붙여줘. 과장된 영입 효과처럼 읽히면 안 돼.

G는 컨트롤 5개(리그/팀/역할/최소출전/포지션) 전부 URL에 반영되게.
```

## 6 — 디자인 전면 개선 (ROADMAP H)

```
ROADMAP.md의 H단계를 해줘. 지금 디자인이 전반적으로 밋밋해서 제대로 손보고 싶어.

H단계에 "지금 뭐가 약한가" 5개랑 방향이 적혀 있으니 그거 기준으로 가되,
너 판단으로 더 나은 방향이 있으면 먼저 제안해줘. 바로 코드 고치지 말고
팔레트·타이포·레이아웃 계획을 먼저 말로 설명해줘. 그거 보고 결정하자.

"절대 깨뜨리면 안 되는 것" 목록은 반드시 지켜줘 — 특히 데이터 로딩 경로 2개랑
다크모드 토큰 구조.

계획 합의되면 구현하고, 라이트/다크 × 데스크톱/모바일 4가지를
실제로 열어서 스크린샷으로 확인한 다음 보여줘.
```

---

## 중간에 쓸 만한 것

**막혔을 때**
```
방금 한 변경 되돌리고, 왜 안 되는지 먼저 진단해줘. 추측 말고 실제로 값을 찍어봐.
```

**검증 다시**
```
CLAUDE.md의 검증 기준으로 지금 data.json 대조해줘.
```

**배포**
```
빌드 다시 돌리고 커밋해서 push해줘. Actions 초록불 뜨는지까지 확인.
```
